// Declaramos el contador
let contador = 100; // Inicializamos en 100

// Sumamos 25 visitantes al contador
contador += 25;

// Le restamos 10 al contador
contador -= 10;

// Multiplicamos el contador por 2
contador = contador * 2;

// Mostramos el resultado 
console.log("CONTADOR FINAL: " + contador );